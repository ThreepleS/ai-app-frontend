window.addEventListener("error", (e) => {  try {    const el = document.querySelector("#log");    if (el) el.textContent = "JS ошибка: " + (e.message || e.error || e);  } catch {}  try {    if ($("#alertModal")) {      $("#alertTitle").textContent = "Ошибка";      $("#alertBody").textContent = "JS ошибка: " + (e.message || e.error || e);      $("#alertModal").classList.add("open");    }  } catch {}  console.error("[global]", e.message || e.error || e);});console.log("[app] script start");const $ = (s) => document.querySelector(s);const log = (t) => {  const el = $("#log");  if (el) el.textContent = t;};if (document.body) {  document.body.setAttribute("data-ran", "1");} else {  console.warn("[app] document.body is null at line 19");}